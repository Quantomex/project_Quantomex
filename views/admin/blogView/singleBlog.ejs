<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
    <!-- Add any additional CSS or styling here -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 20px;
        }

        h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .blog-image {
            max-width: 100%;
            height: auto;
            margin-bottom: 20px;
        }

        .blog-content {
            font-size: 16px;
            line-height: 1.5;
        }

        .back-link {
            display: block;
            margin-top: 20px;
            text-decoration: none;
            color: #007BFF;
        }
    </style>
</head>
<body>
    <h1><%= blog.title %></h1>
    <img class="blog-image" src="<%= blog.image %>" alt="<%= blog.title %>">
    <div class="blog-content">
        <%= blog.data %>
    </div>
    <a class="back-link" href="/blog">Back to Blog Page</a>
</body>
</html>
